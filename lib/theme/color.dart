import 'package:flutter/material.dart';

class AppColor {
  static const bgColor = Color(0xFFf5f5f5);
  static const cardColor = Colors.white;
  static const cardHover = Color.fromARGB(177, 255, 255, 255);
  static const blackPrimary = Colors.black;
  static const blackAlpha45Primary = Colors.black45;
  static const blackIcon = Color(0xFF5c5c5c);
  static const buttonHover = Color.fromARGB(58, 92, 92, 92);

  static const blackDisabled = Color.fromARGB(40, 0, 0, 0);
  static const appBarShadow = Colors.black45;
  static const mainMenuShadow = Colors.black38;
  static const whitePrimary = Colors.white;
  static const redButton = Color(0xFFb91c1c);
  static const whiteButton = Colors.white;
}
